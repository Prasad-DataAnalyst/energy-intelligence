# Core (required)
anthropic>=0.25.0
google-api-python-client>=2.120.0
google-auth>=2.28.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0
feedparser>=6.0.11
pyyaml>=6.0.1
pillow>=10.3.0
requests>=2.31.0
apscheduler>=3.10.4
pytrends>=4.9.2
urllib3>=2.2.0

# Voice providers (optional — system falls back to espeak-ng)
elevenlabs>=1.3.0
edge-tts>=6.1.12
openai>=1.23.0

# Transcription (optional — system falls back to script approximation)
# openai-whisper>=20231117

# Image generation (optional — system falls back to PIL mock)
# replicate>=0.25.0
